#import -> 만들어져 있는 라이브러리를 갖고 오는 것
#as키워드를 이용하여 별칭으로 부를 수 있음.
import random as rd

n = rd.randint(1,10)
print(n)

names = ['lee','park','choi','hong']
rd.shuffle(names)
#names 안에 있는 배열이 랜덤하게 변경이 됨
print(names)
